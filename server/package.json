{
  "name": "uocg-server",
  "version": "0.9.5",
  "private": true,
  "scripts": {
    "start": "node app.js",
    "start:watch": "nodemon app.js",
    "build:docker": "docker build -t itsmatttobin/uocg-server . && docker push itsmatttobin/uocg-server",
    "start:docker": "docker run -p 4000:4000 -d itsmatttobin/uocg-server"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
